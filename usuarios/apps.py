from django.apps import AppConfig


class UsuariosConfig(AppConfig):
    """Configuração do app de usuários e permissões."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "usuarios"
